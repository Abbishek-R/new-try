# config.py

# Number of frames a gesture must be stable before action triggers
STABLE_THRESHOLD = 5  

# Map gestures to actions
GESTURE_MAP = {
    "FIVE": "SELECT",
    "THUMB_UP": "CONFIRM",
    "PEACE": "SEND",
    "FIST": "CANCEL"
}
